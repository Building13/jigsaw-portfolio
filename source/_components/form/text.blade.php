<input
    type="text"
    id="{{ $id }}"
    placeholder="{{ $placeholder ?? "" }}"
    name="{{ $id }}"
    class="block w-full border shadow rounded-lg outline-none mb-2 px-4 py-3 bg-white dark:bg-gray-800 border-gray-300 dark:border-gray-700"
    required
>